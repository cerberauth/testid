{
  "name": "hydra-login-consent",
  "version": "1.0.0",
  "description": "npx tailwindcss init",
  "main": "tailwind.config.js",
  "scripts": {
    "build": "tailwindcss -o ./static/css/main.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.6"
  }
}
